<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.project_test">
   <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Project_test">
        <activity android:name=".user.SignUpActivity2"></activity>
        <activity android:name=".ProfileActivity" />
        <activity android:name=".company.CompanyHome">
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>

            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
        </activity>
        <activity android:name=".user.LocationOrderActivity" />
        <activity android:name=".user.PaymentActivity" />
        <activity android:name=".user.AboutUsActivity" />
        <activity android:name=".user.MyorderActivity" />
        <activity android:name=".user.CompanyActivity" />
        <activity android:name=".user.HomeActivity">

        </activity>
        <activity android:name=".user.SignUpActivity" />
        <activity android:name=".user.NewPassword" />
        <activity android:name=".user.CodeVerification" />
        <activity android:name=".user.SendVerification" />
        <activity android:name=".user.LoginActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".user.GuideActivity" />
        <activity android:name=".user.PolicyActivity" />
        <activity android:name=".user.MainActivity" />
    </application>

</manifest>